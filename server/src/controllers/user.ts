import type { Context, Next } from 'koa';

export const verifyExistenceUsername = () => {};

export const sendSMSVerificationCode = () => {};

export const createUser = (ctx: Context, next: Next) => {};
