import type { Context, Next } from 'koa';

export const verifyExistenceUsername = (ctx: Context, next: Next) => {};

export const sendSMSVerificationCode = (ctx: Context, next: Next) => {};

export const checkSMSVerificationCode = (ctx: Context, next: Next) => {};

export const createUser = (ctx: Context, next: Next) => {};
