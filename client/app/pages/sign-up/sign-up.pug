extends /templates/auth-container/auth-container

block doc-title
  title Sign Up

block auth-form-contents
  p 친구들의 사진과 동영상을 보려면 가입하세요.
  |
  form.auth-form(action="")
    label
      input(required="true" type="text" placeholder="전화번호")
    label
      input(required="true" type="text" placeholder="성명")
    label
      input(required="true" type="text" placeholder="사용자 이름")
    label
      input(required="true" type="password" placeholder="비밀번호")
    button(type="submit") 가입

block separated-contents
  p 계정이 있으신가요? 
    a(href= login) 로그인
